{
  "name": "electron-ts-template",
  "version": "1.0.0",
  "description": "Application built using Electron.js framework.",
  "main": "./build/main.js",
  "scripts": {
    "start": "npm run compile && electron ./build/main.js",
    "compile": "tsc && copyfiles -f index.html build"
  },
  "author": "Mineros04",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "electron": "^16.0.7",
    "typescript": "^4.5.5"
  }
}
